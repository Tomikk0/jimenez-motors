PORT=3000
MARIADB_HOST=localhost
MARIADB_PORT=3306
MARIADB_USER=jimenez_app
MARIADB_PASSWORD=your_password
MARIADB_DATABASE=jimenez_motors
REQUEST_LIMIT=15728640
MYSQL_CLIENT=mysql

# Column aliasing (optional)
# Use the format COLUMN_ALIAS_<table>__<logical_column>=actual_column_name
# Example: uncomment the line below if your `members` table stores the tag name in `member_name`
# COLUMN_ALIAS_members__name=member_name

# Tip: create a dedicated MariaDB user for the app instead of using `root`.
# Example (run in MariaDB):
#   CREATE USER 'jimenez_app'@'%' IDENTIFIED BY 'strong_password';
#   GRANT ALL PRIVILEGES ON jimenez_motors.* TO 'jimenez_app'@'%';
#   FLUSH PRIVILEGES;
